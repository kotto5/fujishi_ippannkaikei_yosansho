# 富士市予算書Excelパーサー 設計ドキュメント

## 1. 対象データの構造

### 1.1 ファイル概要

- ファイル: 令和8年度歳出予算書（.xlsx）
- シート数: 14（款ごとに1シート）
- 合計行数: 約31,800行
- マージセル: 大量（総務費シートだけで30,452個）

### 1.2 シート一覧

| シート名 | 行数 |
|---|---|
| １款＿議会費 | 298 |
| ２款＿総務費 | 5,998 |
| ３款＿民生費 | 4,948 |
| ４款＿衛生費 | 4,198 |
| ５款＿労働費 | 448 |
| ６款＿農林水産業費 | 1,948 |
| ７款＿商工費 | 1,648 |
| ８款＿土木費 | 4,798 |
| ９款＿消防費 | 1,348 |
| 10款＿教育費 | 5,548 |
| 11款＿災害復旧費 | 148 |
| 12款＿公債費 | 148 |
| 13款＿諸支出金費 | 148 |
| 14款＿予備費 | 148 |

### 1.3 レイアウトの本質

1つのシートに**3本の独立した縦ストリーム**が同居した印刷用レイアウト。

**ストリーム① 目レベル（左側）**

| 列 | 内容 |
|---|---|
| C (col 3) | 目番号 |
| E (col 5) | 目名称 |
| L (col 12) | 本年度予算額 |
| O (col 15) | 前年度予算額 |
| R (col 18) | 比較 |
| U (col 21) | 国県支出金 |
| X (col 24) | 地方債 |
| AA (col 27) | その他 |
| AE (col 31) | 一般財源 |

**ストリーム② 節レベル（中央）**

| 列 | 内容 |
|---|---|
| AL (col 38) | 節番号 |
| AN (col 40) | 節名称（複数行にまたがる場合あり） |
| AQ (col 43) | 金額 |

**ストリーム③ 説明レベル（右側）**

| 列 | 内容 |
|---|---|
| AT (col 46) | 説明テキスト（インデントで階層を表現） |
| BR (col 70) | 大事業金額 |
| BN (col 66) | 事業金額 |
| BL (col 64) | 細目金額 |

3ストリームは行位置が独立している。同じ行にデータが並ぶことがあるが、それは偶然であり、行の対応関係に意味はない。

### 1.4 ページヘッダーの繰り返し

印刷用レイアウトのため、シート内にページ区切りが存在し、そのたびにテーブルヘッダーが再挿入される。ページの切り替わりは、C列に `N款　X費` 的なフォーマットのデータが挿入されることで検出できる。

### 1.5 予算書の階層構造

```
款（シート単位）
 └─ 項
     └─ 目
         ├─ 節（→ 細節）
         └─ 説明（大事業 → 事業 → 細目）
```

---

## 2. 設計方針

### 2.1 再帰的分割アプローチ

木構造に沿って**上位から順にチャンクに分割**していく。各レベルの関数は「自分のレベルの境界を見つけてチャンクに切る」ことだけに責任を持ち、チャンク内の処理は下位の関数に委譲する。

ステートマシン（状態変数を上から順に更新しながら走査する方式）は採用しない。再帰分割であれば、各関数が受け取る入力の時点で文脈が確定しており、関数単体でのテストが容易になる。

### 2.2 パターンマッチによるヘッダー検出

ページ境界の検出やヘッダー行の除去は、行数オフセットではなく、セルの値に含まれる既知のカラム名パターンで判定する。小さいシートや将来の空白量変化にも対応できる。

### 2.3 インデント深さのハードコード

説明テキスト（AT列）の階層判定は、先頭全角スペース数のハードコード閾値で行う。想定外のスペース数が出現した場合はエラーで即停止し、必要に応じて金額列ベースの判定に切り替えるかどうかを判断する。

### 2.4 null と 0 の扱い

- `0` = 予算額がゼロ円であると予算書に記載されている
- `null` = 金額欄に何も記載されていない（項目名は存在する）
- レコードの存在自体が「予算書に記載されていた」という事実を表す

年度間比較（OUTER JOIN）時に「項目自体の不在」と「金額の空欄」を混同しないよう注意が必要。これはパーサーの設計ではなく比較結合ステップの責任。パーサー時点では `int | null` で十分。

---

## 3. 処理フロー

### Step 0: シート → 款

```
入力:  workbook
出力:  [(款_code: int, 款_name: str, worksheet)]
```

シート名 `"２款＿総務費"` → `(2, "総務費", ws)` のようにパースする。全角数字→int変換、`"＿"` の右側を款名として取得。

---

### Step 1: 款チャンク → 項チャンクに分割

```
入力:  worksheet の全行（生データ）
出力:  [(項_code: int, 項_name: str, rows: [Row])]
```

**分割の境界検出ルール:**

- E列（col 5）に `N項　XXX` パターンが出現する行を見つける
- 同じ項番号の繰り返し出現（ページヘッダーによる再表示）があるため、**項_code が変化した行だけ**を真の境界とする
- 連続して同じ項_code が出る場合は最初の出現だけを境界として採用
- 境界の位置で行リストを分割

各項チャンクにはヘッダー行も含まれた状態で渡す（次のステップで除去する）。

---

### Step 2: 項チャンク → ページヘッダー除去

```
入力:  1つの項に属する行リスト [Row]
出力:  [Row]（ヘッダー行を除去済み）
```

`is_header_row(row) → bool` のフィルタ関数を適用する。

**除去対象の判定ルール（各行に対して独立に判定）:**

1. **款ヘッダー行:** C列（col 3）に `N款` を含み、かつ `項` も含む行
   - 例: `"　２款　総務費　　１項　総務管理費"`
   - これがページの切り替わりの印
2. **款合計再表示行:** C列に `N款` を含むが `項` を含まない行で、AB列に金額がある
   - 例: `"　２款　総務費"` + AB=9616154
3. **項合計再表示行:** E列に `N項` パターンがあり、AB列に金額がある行
4. **テーブルヘッダー行:** C列の値が `"目"` の行
5. **テーブルヘッダーの続き:** 以下のいずれかの値が出現する行
   - `"本年度予算額"`, `"前年度予算額"`, `"比較"` （L, O, R列）
   - `"特定財源"`, `"一般財源"` （U, AE列）
   - `"国県支出金"`, `"地方債"`, `"その他"` （U, X, AA列）
   - `"区分"`, `"金額"` （AL, AQ列）
   - `"説明"` （AT列）
   - `"千円"` （K, O列等の単位行）
6. **ページフッタ:** A列に `"歳出予算書"` を含む行

---

### Step 3: ヘッダー除去済みの項チャンク → 目チャンクに分割

```
入力:  1つの項に属するヘッダー除去済み行リスト [Row]
出力:  [(目_code: int, 目_name: str, 目メタ: dict, rows: [Row])]

目メタ = {
    本年度: int | None,
    前年度: int | None,
    比較: int | None,
    国県支出金: int | None,
    地方債: int | None,
    その他: int | None,
    一般財源: int | None,
}
```

**分割の境界検出ルール:**

- C列（col 3）に数値が入っていて、かつE列（col 5）に目名称がある行が目の開始行
- 目の開始行からメタデータ（L, O, R, U, X, AA, AE列）を同時に取得
- C列が `"計"` の行は項の合計行。目チャンクには含めず、検算用に別途保持

分割後、各目チャンクには**目の開始行自体も含める**（開始行には節データや説明データが同居している場合があるため）。

---

### Step 4a: 目チャンク → 節の抽出

```
入力:  1つの目に属する行リスト [Row]
出力:  [節レコード]

節レコード = {
    節_code: int,
    節_name: str,        # 複数行にまたがる場合は連結済み
    金額: int | None,
    細節: [
        {name: str, 金額: int | None}
    ]
}
```

**分割 → 処理の構造:**

1. 行リスト全体をスキャンし、AL列（col 38）に数値がある行のインデックスを収集
2. そのインデックスで行リストを分割 → 節チャンク群（各チャンクの先頭行がAL列に番号を持つ）
3. 各節チャンク内の処理:
   - **先頭行:** AL列 → `節_code`、AN列 → `節_name` の先頭部分、AQ列 → `金額`
   - **2行目以降、AN列に値あり かつ AQ列が空:** 節名の継続行。`節_name` に連結
   - **2行目以降、AQ列にも値あり:** 細節レコード `{name: AN列, 金額: AQ列}`

---

### Step 4b: 目チャンク → 説明の抽出

```
入力:  1つの目に属する行リスト [Row]
出力:  ツリー構造:
[
    {
        大事業_code: str | None,
        大事業_name: str,
        金額: int | None,        # BR列
        事業: [
            {
                事業_code: str | None,
                事業_name: str,
                金額: int | None,    # BN列
                細目: [
                    {
                        name: str,
                        金額: int | None,  # BL列
                    }
                ]
            }
        ]
    }
]
```

**階層判定:** AT列テキストの先頭全角スペース数（ハードコード）。

| スペース数 | 階層 | テキスト例 | 金額列 |
|---|---|---|---|
| 0 | 大事業 | `001 給与費` | BR (col 70) |
| 1 | 事業 | `　001 特別職　３人` | BN (col 66) |
| 4 | 細目 | `　　　　全国市長会分担金` | BL (col 64) |
| 5以上 | 説明テキスト（細目の継続行） | `　　　　　身体賠償1人...` | なし |

**想定外のスペース数が出現した場合はエラーで即停止する。**

**分割 → 処理の構造:**

1. AT列に値がある行だけを抽出
2. スペース数 0 の行の位置で分割 → 大事業チャンク群
3. 各大事業チャンク内で、スペース数 1 の行の位置で分割 → 事業チャンク群
4. 各事業チャンク内で、スペース数 4 の行が細目

**金額が空欄の場合の判定:**

- 1つ上のセルに値がある → 前行の金額が複数行にまたがるケース（連結処理の対象）
- 1つ上のセルにも値がない → その列自体が金額を持たない項目。`null` として処理

テキストが `数字列 + スペース + 名前` のパターンにマッチすれば code と name に分離。マッチしなければ code=None の説明行とする。

---

### Step 5: 組み立てと出力

```python
all_目 = []
all_節 = []
all_説明 = []

for sheet in workbook:
    款_code, 款_name = parse_sheet_name(sheet.title)
    項_chunks = split_by_項(sheet)

    for 項_code, 項_name, rows in 項_chunks:
        clean_rows = strip_headers(rows)
        目_chunks = split_by_目(clean_rows)

        for 目_code, 目_name, 目メタ, 目_rows in 目_chunks:
            節_list = extract_節(目_rows)
            説明_tree = extract_説明(目_rows)

            # コンテキストは引数として確定済み（ステートマシン不要）
            context = {款_code, 款_name, 項_code, 項_name, 目_code, 目_name}

            all_目.append({**context, **目メタ})
            all_節.extend([{**context, **節} for 節 in 節_list])
            all_説明.extend(flatten_説明_tree(context, 説明_tree))
```

出力形式は用途に応じて:

- **正規化3テーブル** → DB格納向き
- **目テーブルに節・説明をネストしたJSON** → API・分析向き
- **フラットCSV** → Excel確認向き（ただし節×説明のクロスは意味がないため、どちらかの軸で展開）

---

### Step 6: 検算

- 各目の `本年度` ＝ その目の節レコードの `金額` 合計
- 各目の `本年度` ＝ その目の大事業レコードの BR 金額合計
- 各項の合計行（Step 3 で分離した `計` 行）＝ その項の目の `本年度` 合計

---

## 4. 関数一覧

| 関数 | 入力 | 出力 | 責務 |
|---|---|---|---|
| `parse_sheet_name` | シート名 str | (款_code, 款_name) | シート名から款情報を抽出 |
| `split_by_項` | worksheet | [(項_code, 項_name, [Row])] | E列の `N項` パターンで行を分割 |
| `is_header_row` | Row | bool | カラム名パターンでヘッダー行を判定 |
| `strip_headers` | [Row] | [Row] | ヘッダー行を除去 |
| `split_by_目` | [Row] | [(目_code, 目_name, 目メタ, [Row])] | C列の数値で行を分割、メタ取得 |
| `extract_節` | [Row] | [節レコード] | AL列の数値で分割→節・細節を抽出 |
| `extract_説明` | [Row] | [説明ツリー] | AT列のインデント深さで再帰分割 |
| `validate` | 全テーブル | エラーリスト | 金額の整合性を検算 |
